# Terraform provider plugin cache (hundreds of MB of binaries)
.terraform/

# State files - may contain secrets
*.tfstate
*.tfstate.*
crash.log

# CLI binaries - too large for git, install locally instead
*.exe
